The database setup can be found in app/database/dbscript.sql.
The database was done prior to shifting to Laravel, so no migrations were made.


Remember to update the mysql root password in app/config/database.php.

Usage: This uses basic auth.  Open the users table for usernames.  The passwords are all set to the same as the username for ease of testing.